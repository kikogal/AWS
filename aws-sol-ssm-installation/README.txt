The following solution can be used to connect to any Amazon linux instance and install SSM automatically.

Requirements.
- key .pem to connect to EC2 instance
- User with permissions (normally EC2 user)
